var storage = require('node-persist');

Connection = function(socket) {
  socket.on('acceptmeeting', function() {

  });

  socket.on('session-description', function() {

  });
};

module.exports = Connection;
